---
phase: 02-catalog-and-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/api/src/modules/series/series.routes.ts
  - apps/api/src/modules/series/series.service.ts
  - apps/api/src/modules/categories/categories.routes.ts
  - apps/api/src/modules/categories/categories.service.ts
  - apps/api/src/modules/tags/tags.routes.ts
  - apps/api/src/modules/tags/tags.service.ts
  - apps/api/src/modules/characters/characters.routes.ts
  - apps/api/src/modules/characters/characters.service.ts
  - apps/api/src/create-app.ts
  - apps/api/prisma/seed-taxonomy.ts
autonomous: true
requirements:
  - CATL-03
  - CATL-04
  - SERI-01
  - SERI-02

must_haves:
  truths:
    - "A visitor can list all categories, tags, and characters without authentication"
    - "A visitor can list and search series with pagination"
    - "A visitor can view a series detail with its approved catalog editions"
    - "An admin can create, update, and delete categories, tags, characters, and series"
    - "Non-admin users receive 403 on taxonomy write operations"
    - "Category, tag, and character slugs are auto-generated and unique"
    - "Foundational Brazilian publishers and categories are seeded into the database"
  artifacts:
    - path: "apps/api/src/modules/series/series.service.ts"
      provides: "Series CRUD business logic"
      contains: "listSeries"
    - path: "apps/api/src/modules/series/series.routes.ts"
      provides: "Series REST endpoints"
      contains: "seriesRoutes"
    - path: "apps/api/src/modules/categories/categories.service.ts"
      provides: "Category CRUD business logic"
      contains: "createCategory"
    - path: "apps/api/src/modules/categories/categories.routes.ts"
      provides: "Category REST endpoints"
      contains: "categoriesRoutes"
    - path: "apps/api/src/modules/tags/tags.service.ts"
      provides: "Tag CRUD business logic"
      contains: "createTag"
    - path: "apps/api/src/modules/characters/characters.service.ts"
      provides: "Character CRUD business logic"
      contains: "createCharacter"
    - path: "apps/api/prisma/seed-taxonomy.ts"
      provides: "Seed script for Brazilian publishers and foundational categories"
      contains: "seedTaxonomy"
  key_links:
    - from: "apps/api/src/modules/categories/categories.service.ts"
      to: "apps/api/src/shared/utils/slug.ts"
      via: "uniqueSlug import for auto-slug generation"
      pattern: "import.*uniqueSlug"
    - from: "apps/api/src/create-app.ts"
      to: "apps/api/src/modules/series/series.routes.ts"
      via: "app.use route registration"
      pattern: "app\\.use.*series"
---

<objective>
Create CRUD API modules for all four taxonomy entities: series, categories, tags, and characters. Public read endpoints require no authentication; write operations require admin role.

Purpose: Taxonomy entities (series, categories, tags, characters) must exist before catalog entries can reference them. These modules provide the foundation for catalog entry creation and filtering.

Output: Four API modules (series, categories, tags, characters) with routes and services, registered in create-app.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-and-taxonomy/02-RESEARCH.md
@.planning/phases/02-catalog-and-taxonomy/02-01-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-04-SUMMARY.md
@apps/api/src/modules/auth/auth.routes.ts
@apps/api/src/modules/auth/auth.service.ts
@apps/api/src/create-app.ts
@apps/api/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create series and categories API modules with CRUD endpoints</name>
  <files>
    apps/api/src/modules/series/series.service.ts
    apps/api/src/modules/series/series.routes.ts
    apps/api/src/modules/categories/categories.service.ts
    apps/api/src/modules/categories/categories.routes.ts
  </files>
  <action>
Follow the exact same module pattern as `auth` — routes.ts wires middleware, service.ts has all business logic with Prisma queries.

**Series module (`apps/api/src/modules/series/`):**

`series.service.ts`:
- `listSeries(filters: SeriesSearchInput)`: Prisma findMany with optional `title` contains filter, pagination (skip/take), count for total. Include `_count: { select: { catalogEntries: true } }` to show edition count. Order by title asc.
- `getSeriesById(id: string)`: findUnique with `include: { catalogEntries: { where: { approvalStatus: 'APPROVED' }, orderBy: { editionNumber: 'asc' }, select: { id, title, volumeNumber, editionNumber, coverImageUrl, author, publisher, averageRating, ratingCount } } }`. Throw NotFoundError if not found.
- `createSeries(data: CreateSeriesInput)`: Prisma create.
- `updateSeries(id: string, data: UpdateSeriesInput)`: findUnique first (throw NotFoundError if missing), then update.
- `deleteSeries(id: string)`: findUnique first (throw NotFoundError if missing). Check if series has catalog entries — if so, throw BadRequestError("Cannot delete series with existing catalog entries"). Otherwise delete.

`series.routes.ts`:
- `GET /` — public, validate query with seriesSearchSchema, call listSeries, return with sendPaginated
- `GET /:id` — public, call getSeriesById, return with sendSuccess
- `POST /` — authenticate, authorize('ADMIN'), validate body with createSeriesSchema, call createSeries, return 201
- `PUT /:id` — authenticate, authorize('ADMIN'), validate body with updateSeriesSchema, call updateSeries, return sendSuccess
- `DELETE /:id` — authenticate, authorize('ADMIN'), call deleteSeries, return sendSuccess with `{ deleted: true }`
- Export as `seriesRoutes: Router`

**Categories module (`apps/api/src/modules/categories/`):**

`categories.service.ts`:
- `listCategories()`: findMany ordered by name asc. Include `_count: { select: { catalogEntries: true } }`.
- `getCategoryById(id: string)`: findUnique, throw NotFoundError if missing.
- `createCategory(data: CreateCategoryInput)`: Generate slug via `uniqueSlug(data.name, 'category')`. Prisma create with name, slug, description.
- `updateCategory(id: string, data: UpdateCategoryInput)`: findUnique first. If name is being changed, regenerate slug via `uniqueSlug(data.name, 'category', id)`. Update.
- `deleteCategory(id: string)`: findUnique first. Check if has catalog entries — if so, throw BadRequestError. Otherwise delete.

`categories.routes.ts`:
- `GET /` — public, listCategories, sendSuccess
- `GET /:id` — public, getCategoryById, sendSuccess
- `POST /` — authenticate, authorize('ADMIN'), validate createCategorySchema, create, 201
- `PUT /:id` — authenticate, authorize('ADMIN'), validate updateCategorySchema, update, sendSuccess
- `DELETE /:id` — authenticate, authorize('ADMIN'), delete, sendSuccess
- Export as `categoriesRoutes: Router`

All routes use try/catch wrapping with `next(err)` following the auth module pattern. All service methods import prisma from `../../shared/lib/prisma`. All route exports use explicit `Router` type annotation per 01-04 decision.
  </action>
  <verify>
    Run `pnpm --filter api type-check` — must pass.
    Verify files exist: `ls apps/api/src/modules/series/ apps/api/src/modules/categories/`
  </verify>
  <done>
    Series module has CRUD endpoints (public list/detail with pagination, admin create/update/delete). Categories module has CRUD endpoints with auto-slug generation. Both type-check successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tags and characters API modules and register all four modules in create-app.ts</name>
  <files>
    apps/api/src/modules/tags/tags.service.ts
    apps/api/src/modules/tags/tags.routes.ts
    apps/api/src/modules/characters/characters.service.ts
    apps/api/src/modules/characters/characters.routes.ts
    apps/api/src/create-app.ts
  </files>
  <action>
**Tags module (`apps/api/src/modules/tags/`):**

`tags.service.ts`:
- `listTags()`: findMany ordered by name asc. Include `_count: { select: { catalogEntries: true } }`.
- `getTagById(id: string)`: findUnique, throw NotFoundError.
- `createTag(data: CreateTagInput)`: Generate slug via `uniqueSlug(data.name, 'tag')`. Prisma create with name, slug.
- `updateTag(id: string, data: UpdateTagInput)`: findUnique first. If name changes, regenerate slug. Update.
- `deleteTag(id: string)`: findUnique first. Check catalog entries count — throw BadRequestError if non-zero. Delete.

`tags.routes.ts`:
- Same pattern as categories: GET / (public list), GET /:id (public detail), POST / (admin create with validation), PUT /:id (admin update), DELETE /:id (admin delete)
- Export as `tagsRoutes: Router`

**Characters module (`apps/api/src/modules/characters/`):**

`characters.service.ts`:
- `listCharacters(page?: number, limit?: number)`: findMany with pagination, ordered by name asc. Include `_count: { select: { catalogEntries: true } }`.
- `getCharacterById(id: string)`: findUnique, throw NotFoundError.
- `createCharacter(data: CreateCharacterInput)`: Generate slug via `uniqueSlug(data.name, 'character')`. Prisma create with name, slug, description.
- `updateCharacter(id: string, data: UpdateCharacterInput)`: findUnique first. If name changes, regenerate slug. Update.
- `deleteCharacter(id: string)`: findUnique first. Check catalog entries. Delete.

`characters.routes.ts`:
- GET / (public list with pagination via query params), GET /:id (public detail), POST / (admin), PUT /:id (admin), DELETE /:id (admin)
- Export as `charactersRoutes: Router`

**Register all four modules in `apps/api/src/create-app.ts`:**
Add imports and `app.use` calls AFTER the existing auth and users routes:
```typescript
app.use('/api/v1/series', seriesRoutes);
app.use('/api/v1/categories', categoriesRoutes);
app.use('/api/v1/tags', tagsRoutes);
app.use('/api/v1/characters', charactersRoutes);
```

Verify: `pnpm --filter api type-check` and `pnpm --filter api build` must pass.
  </action>
  <verify>
    Run `pnpm --filter api type-check` — must pass.
    Run `pnpm --filter api build` — must succeed.
    Verify routes registered: grep for 'api/v1/series' and 'api/v1/categories' and 'api/v1/tags' and 'api/v1/characters' in create-app.ts.
  </verify>
  <done>
    All four taxonomy modules (series, categories, tags, characters) have CRUD endpoints with public read / admin write. All registered at /api/v1/{entity}. API builds and type-checks cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create seed script with foundational Brazilian publishers and categories</name>
  <files>
    apps/api/prisma/seed-taxonomy.ts
  </files>
  <action>
Create `apps/api/prisma/seed-taxonomy.ts` — a Prisma seed script that inserts canonical Brazilian comic publishers and foundational categories so the catalog is not empty at phase end.

**Publishers (create as Series or as raw data depending on schema):**
Note: The Prisma schema has `publisher` as a string field on CatalogEntry, not a separate model. So publishers are seeded as categories or stored as reference data. Since the schema uses publisher as a plain string, create a set of **categories** and **tags** that represent the Brazilian comic ecosystem.

**Seed Categories (using the categories service pattern — direct Prisma inserts with slug generation):**
- "Super-herois" (slug: super-herois) — description: "Historias de super-herois e vigilantes"
- "Manga" (slug: manga) — description: "Quadrinhos japoneses traduzidos para o portugues"
- "HQ Nacional" (slug: hq-nacional) — description: "Quadrinhos produzidos por autores brasileiros"
- "Horror" (slug: horror) — description: "Historias de terror e suspense"
- "Ficcao Cientifica" (slug: ficcao-cientifica) — description: "Quadrinhos de ficcao cientifica"
- "Fantasia" (slug: fantasia) — description: "Historias de fantasia e mundos imaginarios"
- "Aventura" (slug: aventura) — description: "Historias de aventura e acao"
- "Romance" (slug: romance) — description: "Historias romanticas e dramas"
- "Comedia" (slug: comedia) — description: "Quadrinhos de humor e comedia"
- "Infantil" (slug: infantil) — description: "Quadrinhos para o publico infantil"
- "Graphic Novel" (slug: graphic-novel) — description: "Narrativas graficas longas e autorais"
- "Underground/Independente" (slug: underground-independente) — description: "Quadrinhos independentes e alternativos"

**Seed Tags:**
- "Panini", "Abril", "Mythos", "Devir", "JBC", "Darkside", "NewPOP", "Conrad", "Pipoca & Nanquim", "Veneta", "Zarabatana", "Nemo", "Mauricio de Sousa"
  These represent the major Brazilian publishers as tags for filtering.

**Seed Characters (popular ones appearing in Brazilian editions):**
- "Homem-Aranha" (slug: homem-aranha)
- "Batman" (slug: batman)
- "Superman" (slug: superman)
- "Wolverine" (slug: wolverine)
- "Monica" (slug: monica) — description: "Turma da Monica"
- "Cebolinha" (slug: cebolinha) — description: "Turma da Monica"
- "Goku" (slug: goku) — description: "Dragon Ball"
- "Naruto" (slug: naruto) — description: "Naruto Shippuden"
- "Luffy" (slug: luffy) — description: "One Piece"

**Implementation:**
- Import PrismaClient
- Use `prisma.category.upsert` with `where: { slug }` to avoid duplicates on re-run
- Use `prisma.tag.upsert` with `where: { slug }` for publisher tags
- Use `prisma.character.upsert` with `where: { slug }` for characters
- Wrap in an async `seedTaxonomy()` function
- Call from the existing `apps/api/prisma/seed.ts` main seed file (add import and call), or add a script entry in package.json: `"db:seed-taxonomy": "tsx prisma/seed-taxonomy.ts"`
- Use `slugify` from the shared utils or inline a simple slugify (lower, replace spaces with hyphens, remove accents) since this runs standalone

Run the seed: `pnpm --filter api db:seed` or `pnpm --filter api tsx prisma/seed-taxonomy.ts`

Verify data exists: connect to DB and check `SELECT COUNT(*) FROM categories`, `SELECT COUNT(*) FROM tags`, `SELECT COUNT(*) FROM characters`.
  </action>
  <verify>
    Run `pnpm --filter api tsx prisma/seed-taxonomy.ts` — must complete without errors.
    Verify seed data: `pnpm --filter api tsx -e "const {PrismaClient} = require('@prisma/client'); const p = new PrismaClient(); p.category.count().then(c => { console.log('Categories:', c); p.$disconnect(); })"` should show >= 12.
  </verify>
  <done>
    Seed script creates 12 foundational categories (Super-herois, Manga, HQ Nacional, Horror, etc.), 13 publisher tags (Panini, Abril, Mythos, Devir, JBC, Darkside, etc.), and 9 popular characters (Homem-Aranha, Batman, Monica, Goku, etc.). Script is idempotent via upsert. Database has initial data for catalog browsing.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter api build` succeeds
- `pnpm --filter api type-check` passes
- All eight new files exist (4 services + 4 routes)
- create-app.ts has all four route registrations
- Public endpoints require no auth; write endpoints require ADMIN role
- Slug auto-generation uses uniqueSlug helper
- Delete operations check for existing catalog references
</verification>

<success_criteria>
- Series CRUD: list with search/pagination, detail with approved editions, admin create/update/delete
- Categories CRUD: list, detail, admin create/update/delete with auto-slug
- Tags CRUD: list, detail, admin create/update/delete with auto-slug
- Characters CRUD: list with pagination, detail, admin create/update/delete with auto-slug
- All routes registered in create-app.ts and API compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-and-taxonomy/02-02-SUMMARY.md`
</output>
