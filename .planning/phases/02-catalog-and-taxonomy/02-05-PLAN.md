---
phase: 02-catalog-and-taxonomy
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/web/src/lib/api/series.ts
  - apps/web/src/lib/api/taxonomy.ts
  - apps/web/src/app/[locale]/(public)/series/page.tsx
  - apps/web/src/app/[locale]/(public)/series/[id]/page.tsx
  - apps/web/src/components/features/series/series-card.tsx
  - apps/web/src/components/features/series/series-editions-list.tsx
  - apps/web/src/messages/pt-BR.json
autonomous: true
requirements:
  - SERI-03
  - SERI-04

must_haves:
  truths:
    - "A visitor can see a series listing page with search and pagination"
    - "A visitor can click a series to see its detail page with all approved editions listed"
    - "Series detail page shows volume/edition numbers for each entry"
    - "All UI text is in PT-BR via next-intl translations"
  artifacts:
    - path: "apps/web/src/app/[locale]/(public)/series/page.tsx"
      provides: "Series listing page with search"
      contains: "SeriesPage"
    - path: "apps/web/src/app/[locale]/(public)/series/[id]/page.tsx"
      provides: "Series detail page with editions"
      contains: "SeriesDetailPage"
    - path: "apps/web/src/lib/api/series.ts"
      provides: "Series API service layer"
      contains: "getSeries"
    - path: "apps/web/src/components/features/series/series-card.tsx"
      provides: "Series card component for listings"
      contains: "SeriesCard"
  key_links:
    - from: "apps/web/src/app/[locale]/(public)/series/page.tsx"
      to: "apps/web/src/lib/api/series.ts"
      via: "API call to fetch series list"
      pattern: "import.*getSeries"
    - from: "apps/web/src/lib/api/series.ts"
      to: "apps/web/src/lib/api/client.ts"
      via: "Axios instance for API requests"
      pattern: "import.*apiClient"
---

<objective>
Build the series listing page (with search and pagination) and series detail page (showing all approved editions) as public frontend pages.

Purpose: Series browsing is a key discovery mechanism — collectors want to see all editions in a series to know what they're missing. This completes the SERI-03 and SERI-04 requirements.

Output: Series listing page, series detail page, series API service, series UI components, PT-BR translations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-and-taxonomy/02-RESEARCH.md
@.planning/phases/02-catalog-and-taxonomy/02-02-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-06b-SUMMARY.md
@apps/web/src/lib/api/client.ts
@apps/web/src/app/[locale]/(public)/layout.tsx
@apps/web/src/messages/pt-BR.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create series API service layer and series listing page with search and pagination</name>
  <files>
    apps/web/src/lib/api/series.ts
    apps/web/src/app/[locale]/(public)/series/page.tsx
    apps/web/src/components/features/series/series-card.tsx
    apps/web/src/messages/pt-BR.json
  </files>
  <action>
**API Service (`apps/web/src/lib/api/series.ts`):**
- Import apiClient from `../client` (the existing axios instance with token refresh interceptor)
- `getSeries(params?: { title?: string; page?: number; limit?: number })`: GET `/api/v1/series` with query params. Return `{ data: Series[], pagination }`.
- `getSeriesById(id: string)`: GET `/api/v1/series/${id}`. Return series with catalogEntries.
- Define TypeScript interfaces locally (or import from contracts if available): `Series { id, title, description, totalEditions, _count?: { catalogEntries: number }, createdAt }`, `SeriesDetail extends Series { catalogEntries: CatalogEdition[] }`, `CatalogEdition { id, title, volumeNumber, editionNumber, coverImageUrl, author, publisher, averageRating, ratingCount }`

**Series Card Component (`apps/web/src/components/features/series/series-card.tsx`):**
- Display: title, description (truncated to 2 lines), edition count badge (e.g., "42 edicoes"), createdAt date
- Use Card component from shadcn/ui (import from @/components/ui/card)
- Link to `/series/${series.id}` using next/link
- Responsive: full width on mobile, card grid on desktop

**Series Listing Page (`apps/web/src/app/[locale]/(public)/series/page.tsx`):**
- Client component ('use client') for interactive search
- Search input at top (shadcn Input component) with debounced search (300ms) — update URL search params
- Use `useSearchParams` from next/navigation to read page/title from URL
- Fetch series via `getSeries({ title, page, limit: 20 })` using useEffect or SWR-style pattern with useState
- Display series as grid of SeriesCard components (3 cols on lg, 2 on md, 1 on sm)
- Pagination controls at bottom: Previous/Next buttons, current page / total pages display
- Show loading skeleton while fetching (use Skeleton from shadcn/ui)
- Show "Nenhuma serie encontrada" empty state when no results
- Page title: "Series" with description text

**Translations (add to apps/web/src/messages/pt-BR.json):**
Add a "series" section with keys:
- `title`: "Series"
- `searchPlaceholder`: "Buscar series..."
- `editions`: "edicoes"
- `noResults`: "Nenhuma serie encontrada"
- `detail.allEditions`: "Todas as Edicoes"
- `detail.edition`: "Edicao"
- `detail.volume`: "Volume"
- `detail.noEditions`: "Nenhuma edicao cadastrada"
- `detail.totalEditions`: "Total de edicoes"

Use `useTranslations('series')` in components for all text.
  </action>
  <verify>
    Run `pnpm --filter web type-check` — must pass.
    Run `pnpm --filter web build` — must succeed.
    Verify files exist: series page, series card, API service.
  </verify>
  <done>
    Series listing page shows searchable, paginated grid of series with title, description, and edition count. API service layer connects to /api/v1/series endpoints. All text in PT-BR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create series detail page showing all editions</name>
  <files>
    apps/web/src/app/[locale]/(public)/series/[id]/page.tsx
    apps/web/src/components/features/series/series-editions-list.tsx
  </files>
  <action>
**Series Editions List Component (`apps/web/src/components/features/series/series-editions-list.tsx`):**
- Props: `editions: CatalogEdition[]`
- Display each edition as a row or small card:
  - Cover image thumbnail (64x96px, placeholder if no coverImageUrl — use a gray box with Book icon from lucide-react)
  - Edition number (e.g., "#3"), volume number if present
  - Title
  - Author and publisher
  - Star rating display (averageRating as filled/empty stars, ratingCount) — show "Sem avaliacoes" if ratingCount is 0
- Link each edition to `/catalog/${edition.id}` (catalog detail page, built in Plan 06)
- Sort by editionNumber ascending (already sorted by API)
- Use Table component from shadcn/ui or a clean list layout

**Series Detail Page (`apps/web/src/app/[locale]/(public)/series/[id]/page.tsx`):**
- Client component that fetches series by ID from API: `getSeriesById(params.id)`
- Display:
  - Series title as h1
  - Description (if present)
  - Progress indicator: "X de Y edicoes no catalogo" (entries count vs totalEditions)
  - SeriesEditionsList component showing all approved editions
- Loading state with Skeleton
- Error state if series not found (show "Serie nao encontrada" message)
- Back link to series listing page
- Use breadcrumb-style navigation: Series > [Series Title]

Use `useTranslations('series')` for all text. Use shadcn/ui components (Card, Table, Badge, Skeleton).
  </action>
  <verify>
    Run `pnpm --filter web type-check` — must pass.
    Run `pnpm --filter web build` — must succeed.
    Verify files exist: series detail page, editions list component.
  </verify>
  <done>
    Series detail page shows series info, progress indicator, and complete list of approved editions with cover thumbnails, edition/volume numbers, and ratings. Links to individual catalog entry detail pages.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` succeeds
- `pnpm --filter web type-check` passes
- Series listing page renders with search input and card grid
- Series detail page renders with editions list
- API service correctly calls /api/v1/series endpoints
- All UI text uses next-intl translations
- Empty states handled (no results, no editions)
</verification>

<success_criteria>
- Series listing page: searchable, paginated, shows edition count per series
- Series detail page: shows title, description, progress (X of Y editions), and all approved editions
- Each edition links to catalog detail page
- All text in PT-BR
- Responsive layout (mobile/tablet/desktop)
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-and-taxonomy/02-05-SUMMARY.md`
</output>
