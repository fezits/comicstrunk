---
phase: 04-marketplace-and-orders
plan: 05
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - apps/web/src/lib/api/marketplace.ts
  - apps/web/src/lib/api/commission.ts
  - apps/web/src/lib/api/cart.ts
  - apps/web/src/lib/api/shipping.ts
  - apps/web/src/lib/api/orders.ts
  - apps/web/src/components/features/marketplace/marketplace-listing-page.tsx
  - apps/web/src/components/features/marketplace/marketplace-card.tsx
  - apps/web/src/components/features/marketplace/marketplace-list-item.tsx
  - apps/web/src/components/features/marketplace/marketplace-filters.tsx
  - apps/web/src/components/features/marketplace/listing-detail.tsx
  - apps/web/src/components/features/marketplace/seller-profile.tsx
  - apps/web/src/app/[locale]/(public)/marketplace/page.tsx
  - apps/web/src/app/[locale]/(public)/marketplace/[id]/page.tsx
  - apps/web/src/app/[locale]/(public)/seller/[id]/page.tsx
  - apps/web/messages/pt-BR.json
autonomous: true
requirements:
  - CART-03
  - CART-06
  - COMM-04

must_haves:
  truths:
    - "A visitor can browse marketplace listings with filters by condition, price range, publisher, and character"
    - "Each listing shows the item title, cover, seller, price, condition, and commission transparency"
    - "A seller sees the real-time commission preview when their item has a sale price"
    - "A listing detail page shows full item info, seller profile link, and an 'Add to Cart' button"
    - "A seller public profile page shows their listed items and rating placeholder"
  artifacts:
    - path: "apps/web/src/lib/api/marketplace.ts"
      provides: "Marketplace API client functions"
    - path: "apps/web/src/lib/api/cart.ts"
      provides: "Cart API client functions"
    - path: "apps/web/src/lib/api/shipping.ts"
      provides: "Shipping/address API client functions"
    - path: "apps/web/src/lib/api/orders.ts"
      provides: "Orders API client functions"
    - path: "apps/web/src/lib/api/commission.ts"
      provides: "Commission API client functions"
    - path: "apps/web/src/components/features/marketplace/marketplace-listing-page.tsx"
      provides: "Main marketplace browse page component"
    - path: "apps/web/src/app/[locale]/(public)/marketplace/page.tsx"
      provides: "Marketplace page route"
    - path: "apps/web/src/app/[locale]/(public)/marketplace/[id]/page.tsx"
      provides: "Listing detail route"
  key_links:
    - from: "apps/web/src/lib/api/marketplace.ts"
      to: "/api/v1/collection"
      via: "Fetches for-sale items from collection endpoint filtered by isForSale=true"
      pattern: "apiClient.*collection"
    - from: "apps/web/src/lib/api/cart.ts"
      to: "/api/v1/cart"
      via: "Cart CRUD operations"
      pattern: "apiClient.*/cart"
    - from: "apps/web/src/app/[locale]/(public)/marketplace/page.tsx"
      to: "marketplace-listing-page.tsx"
      via: "Page renders the listing component"
      pattern: "MarketplaceListingPage"
---

<objective>
Build the marketplace browse UI and all frontend API clients for Phase 4: marketplace listing page with filters, listing detail page, seller public profile, and API client modules for cart, orders, shipping, and commission.

Purpose: This is the public-facing marketplace that transforms the platform from a private collection tool into a peer-to-peer selling platform. Buyers need to discover items, see prices and commission transparency, and access listing details.
Output: Marketplace pages (browse + detail + seller profile), 5 API client modules, i18n translations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketplace-and-orders/04-RESEARCH.md
@.planning/phases/04-marketplace-and-orders/04-01-SUMMARY.md
@apps/web/src/lib/api/catalog.ts
@apps/web/src/components/features/catalog/
@apps/web/messages/pt-BR.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend API clients for cart, orders, shipping, commission, and marketplace</name>
  <files>
    apps/web/src/lib/api/marketplace.ts
    apps/web/src/lib/api/cart.ts
    apps/web/src/lib/api/shipping.ts
    apps/web/src/lib/api/orders.ts
    apps/web/src/lib/api/commission.ts
  </files>
  <action>
    Follow the same pattern as existing API clients (catalog.ts, collection.ts): import apiClient, define typed functions, export.

    **apps/web/src/lib/api/marketplace.ts:**
    - `searchMarketplace(params)`: GET /api/v1/collection with isForSale=true plus filter params (condition, minPrice, maxPrice, publisher, characterId, query, sortBy, sortOrder, page, limit). This reuses the existing collection list endpoint filtered for items that are for sale.
    - `getMarketplaceListing(id)`: GET /api/v1/collection/items/:id — gets a specific collection item that is for sale
    - `getSellerListings(sellerId, params)`: GET /api/v1/collection with userId filter and isForSale=true
    - `getSellerProfile(sellerId)`: GET /api/v1/users/:sellerId/profile (public profile)

    **apps/web/src/lib/api/cart.ts:**
    - `addToCart(collectionItemId)`: POST /api/v1/cart
    - `getCart()`: GET /api/v1/cart
    - `getCartSummary()`: GET /api/v1/cart/summary
    - `removeFromCart(cartItemId)`: DELETE /api/v1/cart/:id
    - `clearCart()`: DELETE /api/v1/cart

    **apps/web/src/lib/api/shipping.ts:**
    - `createAddress(data)`: POST /api/v1/shipping/addresses
    - `listAddresses()`: GET /api/v1/shipping/addresses
    - `getAddress(id)`: GET /api/v1/shipping/addresses/:id
    - `updateAddress(id, data)`: PUT /api/v1/shipping/addresses/:id
    - `deleteAddress(id)`: DELETE /api/v1/shipping/addresses/:id
    - `setDefaultAddress(id)`: PATCH /api/v1/shipping/addresses/:id/default
    - `listShippingMethods()`: GET /api/v1/shipping/methods
    - `updateTracking(orderItemId, data)`: PATCH /api/v1/shipping/tracking/:orderItemId

    **apps/web/src/lib/api/orders.ts:**
    - `createOrder(shippingAddressId)`: POST /api/v1/orders
    - `listBuyerOrders(params)`: GET /api/v1/orders/buyer
    - `listSellerOrders(params)`: GET /api/v1/orders/seller
    - `getOrder(id)`: GET /api/v1/orders/:id
    - `getOrderByNumber(orderNumber)`: GET /api/v1/orders/number/:orderNumber
    - `cancelOrder(id)`: PATCH /api/v1/orders/:id/cancel
    - `updateOrderItemStatus(itemId, status)`: PATCH /api/v1/orders/items/:itemId/status

    **apps/web/src/lib/api/commission.ts:**
    - `previewCommission(price)`: GET /api/v1/commission/preview?price=X
    - `listCommissionConfigs()`: GET /api/v1/commission/configs (admin)
    - `createCommissionConfig(data)`: POST /api/v1/commission/configs (admin)
    - `updateCommissionConfig(id, data)`: PUT /api/v1/commission/configs/:id (admin)
  </action>
  <verify>
    Run `pnpm --filter web build` or `pnpm type-check` -- should compile.
    All API functions typed and consistent with existing client patterns.
  </verify>
  <done>
    Five API client modules created with full type safety, following existing patterns from catalog.ts and collection.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build marketplace browse, listing detail, and seller profile pages</name>
  <files>
    apps/web/src/components/features/marketplace/marketplace-listing-page.tsx
    apps/web/src/components/features/marketplace/marketplace-card.tsx
    apps/web/src/components/features/marketplace/marketplace-list-item.tsx
    apps/web/src/components/features/marketplace/marketplace-filters.tsx
    apps/web/src/components/features/marketplace/listing-detail.tsx
    apps/web/src/components/features/marketplace/seller-profile.tsx
    apps/web/src/app/[locale]/(public)/marketplace/page.tsx
    apps/web/src/app/[locale]/(public)/marketplace/[id]/page.tsx
    apps/web/src/app/[locale]/(public)/seller/[id]/page.tsx
    apps/web/messages/pt-BR.json
  </files>
  <action>
    Follow the existing catalog browse UI patterns (catalog-listing-page, catalog-card, catalog-filters):

    **marketplace-listing-page.tsx:**
    - Client component with URL search params sync for filter state persistence (same pattern as catalog)
    - Calls searchMarketplace with current filters
    - Grid/list view toggle (reuse pattern from catalog)
    - Search input with 400ms debounce
    - Pagination component
    - Empty state: "Nenhum item a venda encontrado"

    **marketplace-card.tsx:**
    - Card showing: cover image, title, condition badge, price in BRL (R$ XX,XX), seller name, "Adicionar ao Carrinho" button
    - Condition uses ItemCondition mapped to PT-BR labels and color-coded badges
    - Price formatted with Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })
    - Click card navigates to listing detail

    **marketplace-list-item.tsx:**
    - Horizontal list view variant with same info

    **marketplace-filters.tsx:**
    - Collapsible panel (desktop) / Sheet drawer (mobile) — same pattern as catalog
    - Filters: condition dropdown (NEW, VERY_GOOD, GOOD, FAIR, POOR), price range (min/max inputs), publisher text, character select
    - Sort: price asc/desc, newest, condition

    **listing-detail.tsx:**
    - Full listing page: large cover image, title, series/edition info, condition, price
    - Commission transparency: "Comissao da plataforma: R$ X,XX | Vendedor recebe: R$ X,XX" — call previewCommission to get these values
    - Seller info section with link to seller profile
    - "Adicionar ao Carrinho" button (disabled if own item, shows toast on success/error)
    - Show "Item ja reservado" if item is already in someone's cart (API returns this status)

    **seller-profile.tsx:**
    - Public seller page showing: display name, member since, item count for sale
    - List of seller's active listings with marketplace-card
    - Rating placeholder (Phase 7): "Avaliacoes em breve"

    **Page routes:**
    - marketplace/page.tsx: Render MarketplaceListingPage
    - marketplace/[id]/page.tsx: Render ListingDetail, passing id from params
    - seller/[id]/page.tsx: Render SellerProfile, passing id from params

    **i18n (apps/web/messages/pt-BR.json):**
    Add `marketplace` section with all strings:
    - title: "Marketplace", search: "Buscar no marketplace", filters: "Filtros", condition: "Condicao", price: "Preco", priceRange: "Faixa de preco", minPrice: "Preco minimo", maxPrice: "Preco maximo", seller: "Vendedor", addToCart: "Adicionar ao Carrinho", itemReserved: "Item reservado", ownItem: "Voce nao pode comprar seu proprio item", commission: "Comissao", sellerReceives: "Vendedor recebe", noResults: "Nenhum item a venda encontrado", sellerProfile: "Perfil do Vendedor", memberSince: "Membro desde", itemsForSale: "Itens a venda", ratingsComingSoon: "Avaliacoes em breve"
    Also add condition labels: NEW: "Novo", VERY_GOOD: "Muito Bom", GOOD: "Bom", FAIR: "Regular", POOR: "Ruim"
  </action>
  <verify>
    Run `pnpm --filter web build` -- should compile.
    Navigate to /pt-BR/marketplace -- should show listing page with filters.
    Navigate to /pt-BR/marketplace/[id] -- should show listing detail.
    Navigate to /pt-BR/seller/[id] -- should show seller profile.
  </verify>
  <done>
    Marketplace browse with filters, listing detail with commission transparency, and seller profile all rendering. All text in PT-BR via next-intl. Grid/list toggle working. URL search params synced for shareable filter URLs.
  </done>
</task>

</tasks>

<verification>
- Marketplace page loads with empty state or listings
- Filters update URL params and trigger API calls
- Listing detail shows commission transparency
- Seller profile page renders with listings
- All strings in PT-BR
- API clients typed and consistent with existing patterns
</verification>

<success_criteria>
- Public marketplace browse page with filter sidebar and search
- Listing detail with price, condition, commission display, and cart button
- Seller public profile with their listings
- All 5 API client modules created and typed
- i18n translations complete for marketplace section
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketplace-and-orders/04-05-SUMMARY.md`
</output>
