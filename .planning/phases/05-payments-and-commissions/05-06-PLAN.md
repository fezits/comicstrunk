---
phase: 05-payments-and-commissions
plan: 06
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
files_modified:
  - apps/web/src/lib/api/admin-payments.ts
  - apps/web/src/lib/api/admin-commission.ts
  - apps/web/src/components/features/admin/payments/admin-payments-page.tsx
  - apps/web/src/components/features/admin/payments/pending-payments-table.tsx
  - apps/web/src/components/features/admin/payments/payment-approval-dialog.tsx
  - apps/web/src/components/features/admin/commission/admin-commission-page.tsx
  - apps/web/src/components/features/admin/commission/commission-summary-cards.tsx
  - apps/web/src/components/features/admin/commission/commission-transactions-table.tsx
  - apps/web/src/components/features/admin/banking/admin-banking-page.tsx
  - apps/web/src/app/[locale]/(admin)/admin/payments/page.tsx
  - apps/web/src/app/[locale]/(admin)/admin/commission/page.tsx
  - apps/web/src/app/[locale]/(admin)/admin/banking/page.tsx
  - apps/web/messages/pt-BR.json
autonomous: true
requirements:
  - PYMT-03
  - PYMT-08
  - COMM-06
  - BANK-03

must_haves:
  truths:
    - "An admin can see a list of pending payments and approve or reject each one"
    - "An admin can view commission totals by period and by plan rate"
    - "An admin can view a paginated list of individual commission transactions"
    - "An admin can view all sellers' bank accounts with seller info for payout processing"
  artifacts:
    - path: "apps/web/src/lib/api/admin-payments.ts"
      provides: "Admin payment API client functions"
    - path: "apps/web/src/lib/api/admin-commission.ts"
      provides: "Admin commission API client functions"
    - path: "apps/web/src/components/features/admin/payments/admin-payments-page.tsx"
      provides: "Admin payment dashboard with pending approvals and all payments"
    - path: "apps/web/src/components/features/admin/commission/admin-commission-page.tsx"
      provides: "Admin commission dashboard with period selector and totals"
    - path: "apps/web/src/components/features/admin/banking/admin-banking-page.tsx"
      provides: "Admin view of all sellers' bank accounts"
    - path: "apps/web/src/app/[locale]/(admin)/admin/payments/page.tsx"
      provides: "Admin payments page route"
    - path: "apps/web/src/app/[locale]/(admin)/admin/commission/page.tsx"
      provides: "Admin commission page route"
    - path: "apps/web/src/app/[locale]/(admin)/admin/banking/page.tsx"
      provides: "Admin banking page route"
  key_links:
    - from: "apps/web/src/lib/api/admin-payments.ts"
      to: "/api/v1/payments/admin/*"
      via: "Admin payment API calls"
      pattern: "apiClient\\.(get|post).*payments/admin"
    - from: "apps/web/src/lib/api/admin-commission.ts"
      to: "/api/v1/commission/admin/*"
      via: "Admin commission API calls"
      pattern: "apiClient\\.get.*commission/admin"
    - from: "apps/web/src/components/features/admin/banking/admin-banking-page.tsx"
      to: "/api/v1/banking/admin/list"
      via: "Admin banking list API call"
      pattern: "apiClient\\.get.*banking/admin"
---

<objective>
Build the admin-facing payment, commission, and banking dashboard pages: pending payment approvals with approve/reject actions, commission reporting with period selector and plan breakdowns, and seller bank account list for payout processing.

Purpose: Admins need financial visibility and control. They must be able to intervene on stuck payments, monitor commission revenue, and access seller bank details for manual payout processing.

Output: Three admin pages at /admin/payments, /admin/commission, /admin/banking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-payments-and-commissions/05-RESEARCH.md
@.planning/phases/05-payments-and-commissions/05-01-SUMMARY.md
@.planning/phases/05-payments-and-commissions/05-02-SUMMARY.md
@.planning/phases/05-payments-and-commissions/05-03-SUMMARY.md
@apps/web/src/lib/api/orders.ts
@apps/web/src/lib/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin API clients and payment dashboard page</name>
  <files>
    apps/web/src/lib/api/admin-payments.ts
    apps/web/src/lib/api/admin-commission.ts
    apps/web/src/components/features/admin/payments/admin-payments-page.tsx
    apps/web/src/components/features/admin/payments/pending-payments-table.tsx
    apps/web/src/components/features/admin/payments/payment-approval-dialog.tsx
    apps/web/src/app/[locale]/(admin)/admin/payments/page.tsx
    apps/web/messages/pt-BR.json
  </files>
  <action>
1. Create `apps/web/src/lib/api/admin-payments.ts`:
   - Import `apiClient` from `./client`.
   - Types:
     ```typescript
     export interface PendingPaymentOrder {
       id: string;
       orderNumber: string;
       status: string;
       totalAmount: number;
       createdAt: string;
       buyer: { name: string; email: string };
       payments: Array<{
         id: string;
         method: string;
         providerStatus: string | null;
         amount: number;
         pixExpiresAt: string | null;
         createdAt: string;
       }>;
     }
     export interface AdminPayment {
       id: string;
       orderId: string;
       method: string;
       providerPaymentId: string | null;
       providerStatus: string | null;
       amount: number;
       paidAt: string | null;
       refundedAmount: number | null;
       createdAt: string;
       order: { orderNumber: string; status: string; totalAmount: number; createdAt: string };
       // buyer info from order
     }
     ```
   - `getAdminPendingPayments(page?: number, limit?: number)` — GET `/payments/admin/pending`.
   - `getAdminAllPayments(params?: { status?: string; page?: number; limit?: number })` — GET `/payments/admin/list`.
   - `adminApprovePayment(orderId: string)` — POST `/payments/admin/approve` with `{ orderId }`.
   - `adminRejectPayment(orderId: string)` — POST `/payments/admin/reject` with `{ orderId }`.
   - `adminRefundPayment(paymentId: string, amount?: number)` — POST `/payments/${paymentId}/refund` with `{ amount }` (omit amount for full refund).

2. Create `apps/web/src/lib/api/admin-commission.ts`:
   - Types:
     ```typescript
     export interface CommissionDashboardData {
       byPlan: Array<{
         rate: number;
         transaction_count: number;
         total_commission: number;
         total_sales: number;
       }>;
       totals: { totalCommission: number; totalSales: number; transactionCount: number };
       period: { start: string; end: string };
     }
     export interface CommissionTransaction {
       id: string;
       orderId: string;
       orderNumber: string;
       priceSnapshot: number;
       commissionRateSnapshot: number;
       commissionAmountSnapshot: number;
       sellerNetSnapshot: number;
       status: string;
       createdAt: string;
       catalogTitle: string;
     }
     ```
   - `getCommissionDashboard(periodStart: string, periodEnd: string)` — GET `/commission/admin/dashboard` with query params.
   - `getCommissionTransactions(params?: { periodStart?: string; periodEnd?: string; page?: number; limit?: number })` — GET `/commission/admin/transactions`.

3. Create `apps/web/src/components/features/admin/payments/pending-payments-table.tsx`:
   - Props: `{ orders: PendingPaymentOrder[]; onApprove: (orderId: string) => void; onReject: (orderId: string) => void; loading: boolean }`.
   - Render shadcn Table with columns:
     - Pedido (orderNumber)
     - Comprador (buyer name + email)
     - Valor (totalAmount formatted as BRL)
     - Data (createdAt, formatted DD/MM/YYYY HH:MM)
     - PIX Status (providerStatus badge from payment record if exists)
     - Acoes (Aprovar button green, Rejeitar button red)
   - Each row expandable or with tooltip showing PIX expiry info.
   - Empty state: "Nenhum pagamento pendente" with a CheckCircle icon.

4. Create `apps/web/src/components/features/admin/payments/payment-approval-dialog.tsx`:
   - Props: `{ type: 'approve' | 'reject'; orderNumber: string; amount: number; open: boolean; onConfirm: () => void; onCancel: () => void; loading: boolean }`.
   - For approve: "Confirmar pagamento de {amount} para o pedido #{orderNumber}?"
   - For reject: "Rejeitar pagamento do pedido #{orderNumber}? O pedido sera cancelado."
   - Confirm and Cancel buttons. Loading state on Confirm.

5. Create `apps/web/src/components/features/admin/payments/admin-payments-page.tsx`:
   - Client component with two tabs (shadcn Tabs): "Pendentes" and "Todos".
   - **Pendentes tab**: Fetch `getAdminPendingPayments()`. Show PendingPaymentsTable.
     - On approve click: open PaymentApprovalDialog, on confirm call `adminApprovePayment(orderId)`, refresh list, show toast "Pagamento aprovado".
     - On reject click: open PaymentApprovalDialog (reject type), on confirm call `adminRejectPayment(orderId)`, refresh list, show toast "Pagamento rejeitado".
   - **Todos tab**: Fetch `getAdminAllPayments()` with pagination. Show table with all payments:
     - Columns: Pedido, Valor, Metodo, Status (badge), Data pagamento, Reembolso.
     - Pagination controls.
   - Page title: "Pagamentos".

6. Create `apps/web/src/app/[locale]/(admin)/admin/payments/page.tsx`:
   - Render `<AdminPaymentsPage />`.

7. Update `apps/web/messages/pt-BR.json`:
   - Add `"adminPayments"` key with translations:
     - `title`: "Pagamentos"
     - `pending`: "Pendentes"
     - `all`: "Todos"
     - `buyer`: "Comprador"
     - `order`: "Pedido"
     - `amount`: "Valor"
     - `date`: "Data"
     - `method`: "Metodo"
     - `status`: "Status"
     - `paidAt`: "Data pagamento"
     - `refund`: "Reembolso"
     - `actions`: "Acoes"
     - `approve`: "Aprovar"
     - `reject`: "Rejeitar"
     - `noPending`: "Nenhum pagamento pendente"
     - `noPayments`: "Nenhum pagamento encontrado"
     - `confirmApprove`: "Confirmar pagamento de {amount} para o pedido #{order}?"
     - `confirmReject`: "Rejeitar pagamento do pedido #{order}? O pedido sera cancelado."
     - `paymentApproved`: "Pagamento aprovado com sucesso"
     - `paymentRejected`: "Pagamento rejeitado"
     - `pixStatus`: "Status PIX"
     - `statusApproved`: "Confirmado"
     - `statusPending`: "Pendente"
     - `statusRejected`: "Rejeitado"
     - `statusRefunded`: "Reembolsado"
  </action>
  <verify>
    - All files exist in the specified paths
    - `pnpm --filter web build` succeeds (or `pnpm --filter web lint` passes)
    - admin-payments.ts has all API functions (pending, all, approve, reject, refund)
    - admin-commission.ts has dashboard and transactions API functions
    - AdminPaymentsPage has two tabs (Pendentes and Todos)
    - Approval dialog has confirm/cancel with loading state
    - pt-BR.json has adminPayments translations
  </verify>
  <done>
    Admin can view pending payments in a table, approve or reject each one via confirmation dialog, and browse all payments with pagination. API clients for admin payment and commission endpoints are ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin commission dashboard and banking pages</name>
  <files>
    apps/web/src/components/features/admin/commission/admin-commission-page.tsx
    apps/web/src/components/features/admin/commission/commission-summary-cards.tsx
    apps/web/src/components/features/admin/commission/commission-transactions-table.tsx
    apps/web/src/components/features/admin/banking/admin-banking-page.tsx
    apps/web/src/app/[locale]/(admin)/admin/commission/page.tsx
    apps/web/src/app/[locale]/(admin)/admin/banking/page.tsx
    apps/web/messages/pt-BR.json
  </files>
  <action>
1. Create `apps/web/src/components/features/admin/commission/commission-summary-cards.tsx`:
   - Props: `{ data: CommissionDashboardData | null; loading: boolean }`.
   - Three summary cards (shadcn Card):
     - "Total Comissoes" — sum of all commissions in period, formatted as BRL. Green accent.
     - "Volume de Vendas" — sum of all sales, formatted as BRL. Blue accent.
     - "Transacoes" — transaction count. Purple accent.
   - Below cards: breakdown table by plan rate:
     - Rate (formatted as percentage, e.g., "10%", "8%")
     - Transactions (count)
     - Commission (BRL)
     - Sales volume (BRL)
   - Loading state: Skeleton cards.

2. Create `apps/web/src/components/features/admin/commission/commission-transactions-table.tsx`:
   - Props: `{ transactions: CommissionTransaction[]; pagination: PaginationMeta; onPageChange: (page: number) => void; loading: boolean }`.
   - Shadcn Table with columns:
     - Pedido (orderNumber, link to /orders/{orderId})
     - Item (catalogTitle)
     - Preco (priceSnapshot as BRL)
     - Taxa (commissionRateSnapshot as percentage)
     - Comissao (commissionAmountSnapshot as BRL)
     - Liquido vendedor (sellerNetSnapshot as BRL)
     - Status (order item status badge)
     - Data (createdAt)
   - Pagination controls.
   - Loading: Skeleton rows.

3. Create `apps/web/src/components/features/admin/commission/admin-commission-page.tsx`:
   - Client component.
   - Period selector at top: two date inputs (shadcn Input type="date") for start and end date. Default: current month (first day to today).
   - "Filtrar" button to apply date range.
   - On mount and on filter apply: fetch `getCommissionDashboard(start, end)` and `getCommissionTransactions({ periodStart: start, periodEnd: end })`.
   - Render CommissionSummaryCards with dashboard data.
   - Below: CommissionTransactionsTable with transaction list.
   - Page title: "Comissoes".

4. Create `apps/web/src/components/features/admin/banking/admin-banking-page.tsx`:
   - Client component.
   - Fetch bank accounts: create a simple inline API call using `apiClient.get('/banking/admin/list', { params: { page, limit } })` — or import from admin-payments.ts if you add it there. Better: add `getAdminBankAccounts(params)` to `admin-payments.ts`.
   - Add to `apps/web/src/lib/api/admin-payments.ts`:
     ```typescript
     export interface AdminBankAccount {
       id: string;
       bankName: string;
       branchNumber: string;
       accountNumber: string;
       cpf: string;
       holderName: string;
       accountType: 'CHECKING' | 'SAVINGS';
       isPrimary: boolean;
       createdAt: string;
       user: { name: string; email: string };
     }
     export async function getAdminBankAccounts(params?: { userId?: string; page?: number; limit?: number }) { ... }
     ```
   - Display as shadcn Table:
     - Columns: Vendedor (user name + email), Banco, Agencia, Conta (partially masked: show last 4 digits), CPF (partially masked: show first 3 and last 2), Titular, Tipo, Principal (Badge).
   - Optional filter: search by seller name/email (client-side filter on loaded data, or use userId param if available).
   - Pagination controls.
   - Empty state: "Nenhuma conta bancaria cadastrada por vendedores".
   - Page title: "Contas Bancarias dos Vendedores".

5. Create `apps/web/src/app/[locale]/(admin)/admin/commission/page.tsx`:
   - Render `<AdminCommissionPage />`.

6. Create `apps/web/src/app/[locale]/(admin)/admin/banking/page.tsx`:
   - Render `<AdminBankingPage />`.

7. Update `apps/web/messages/pt-BR.json`:
   - Add `"adminCommission"` key:
     - `title`: "Comissoes"
     - `totalCommission`: "Total Comissoes"
     - `salesVolume`: "Volume de Vendas"
     - `transactions`: "Transacoes"
     - `periodStart`: "Data inicial"
     - `periodEnd`: "Data final"
     - `filter`: "Filtrar"
     - `byPlan`: "Por taxa"
     - `rate`: "Taxa"
     - `count`: "Quantidade"
     - `commission`: "Comissao"
     - `sales`: "Vendas"
     - `order`: "Pedido"
     - `item`: "Item"
     - `price`: "Preco"
     - `sellerNet`: "Liquido vendedor"
     - `status`: "Status"
     - `date`: "Data"
     - `noTransactions`: "Nenhuma transacao no periodo"
   - Add `"adminBanking"` key:
     - `title`: "Contas Bancarias dos Vendedores"
     - `seller`: "Vendedor"
     - `bank`: "Banco"
     - `branch`: "Agencia"
     - `account`: "Conta"
     - `cpf`: "CPF"
     - `holder`: "Titular"
     - `type`: "Tipo"
     - `primary`: "Principal"
     - `checking`: "Corrente"
     - `savings`: "Poupanca"
     - `noAccounts`: "Nenhuma conta bancaria cadastrada por vendedores"
  </action>
  <verify>
    - All files exist in specified paths
    - `pnpm --filter web build` succeeds (or `pnpm --filter web lint` passes)
    - Commission page has period selector and summary cards
    - Commission transactions table has all columns with BRL formatting
    - Admin banking page shows partially masked sensitive data (account number, CPF)
    - All admin pages at correct routes under (admin) group
    - pt-BR.json has adminCommission and adminBanking translations
  </verify>
  <done>
    Admin commission dashboard shows totals by plan and period with transaction list. Admin banking page shows all sellers' bank accounts with masked sensitive data. All three admin pages accessible at /admin/payments, /admin/commission, /admin/banking.
  </done>
</task>

</tasks>

<verification>
1. Frontend build: `pnpm --filter contracts build && pnpm --filter web build` passes
2. Admin payments page has pending tab with approve/reject and all tab with pagination
3. Commission dashboard aggregates by plan rate and shows period totals
4. Bank account data is partially masked in admin view for security
5. All admin routes under (admin) route group require admin access
6. All UI strings internationalized in pt-BR.json
7. BRL formatting uses Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })
</verification>

<success_criteria>
- /admin/payments shows pending payments tab with approve/reject buttons that trigger confirmation dialogs
- /admin/payments "Todos" tab shows paginated list of all payments
- /admin/commission shows summary cards (total commissions, sales volume, transaction count) for selected period
- /admin/commission shows transaction list with order links, prices, rates, and commission amounts
- /admin/banking shows paginated list of all seller bank accounts with masked sensitive data
</success_criteria>

<output>
After completion, create `.planning/phases/05-payments-and-commissions/05-06-SUMMARY.md`
</output>
