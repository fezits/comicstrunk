---
phase: 01-foundation-and-infrastructure
plan: 06
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-05
files_modified:
  - apps/web/next.config.ts
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
  - apps/web/components.json
  - apps/web/src/styles/globals.css
  - apps/web/src/i18n/routing.ts
  - apps/web/src/i18n/request.ts
  - apps/web/src/messages/pt-BR.json
  - apps/web/src/middleware.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/app/not-found.tsx
autonomous: true
requirements:
  - INFRA-07
  - INFRA-09

must_haves:
  truths:
    - "The Next.js frontend loads with dark mode active by default"
    - "A theme toggle switches between dark and light mode, and the choice persists across browser refreshes"
    - "All text is rendered in PT-BR using next-intl translations"
    - "shadcn/ui components are available and styled with the purple accent color (#7C3AED)"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout with html/body and suppressHydrationWarning"
      contains: "suppressHydrationWarning"
    - path: "apps/web/src/app/[locale]/layout.tsx"
      provides: "Locale layout with ThemeProvider and NextIntlClientProvider"
      contains: "ThemeProvider"
    - path: "apps/web/src/i18n/routing.ts"
      provides: "next-intl routing configuration"
      contains: "defineRouting"
    - path: "apps/web/src/messages/pt-BR.json"
      provides: "PT-BR translation messages"
      contains: "pt-BR"
    - path: "apps/web/src/styles/globals.css"
      provides: "Tailwind directives and shadcn CSS variables with purple theme"
      contains: "--primary: 263"
    - path: "apps/web/src/middleware.ts"
      provides: "next-intl locale middleware"
      contains: "createMiddleware"
  key_links:
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/styles/globals.css"
      via: "CSS import"
      pattern: "globals.css"
    - from: "apps/web/src/middleware.ts"
      to: "apps/web/src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware"
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/src/messages/pt-BR.json"
      via: "NextIntlClientProvider"
      pattern: "NextIntlClientProvider"
---

<objective>
Configure the Next.js 15 frontend with next-intl i18n (PT-BR), next-themes dark/light toggle with purple accent, Tailwind CSS, and shadcn/ui component library initialization.

Purpose: This delivers the core frontend configuration — theming, translations, CSS framework, and component library — that Plan 01-06b builds the layout system on top of.
Output: A Next.js app with dark mode default, PT-BR text, purple accent shadcn/ui components, and all essential configuration in place.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Next.js with next-intl, next-themes, Tailwind, and shadcn/ui</name>
  <files>
    apps/web/next.config.ts
    apps/web/tailwind.config.ts
    apps/web/postcss.config.js
    apps/web/components.json
    apps/web/src/styles/globals.css
    apps/web/src/i18n/routing.ts
    apps/web/src/i18n/request.ts
    apps/web/src/messages/pt-BR.json
    apps/web/src/middleware.ts
    apps/web/src/app/layout.tsx
    apps/web/src/app/[locale]/layout.tsx
    apps/web/src/app/not-found.tsx
  </files>
  <action>
    **Install dependencies (if not done in 01-01):**
    ```
    pnpm --filter web add next-intl next-themes lucide-react sonner class-variance-authority clsx tailwind-merge
    pnpm --filter web add -D @tailwindcss/typography
    ```

    **Initialize shadcn/ui:**
    Run `pnpm --filter web dlx shadcn@latest init` with:
    - Style: Default
    - Base color: Slate (will customize to match dark vault vibe)
    - CSS variables: Yes
    - Aliases: @/components, @/lib, @/hooks

    Then install essential shadcn/ui components:
    ```
    pnpm --filter web dlx shadcn@latest add button card input label form toast sheet dialog dropdown-menu avatar separator skeleton badge
    ```

    **Tailwind configuration (`tailwind.config.ts`):**
    - darkMode: "class"
    - content: ["./src/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"]
    - Extend theme: container centered with padding, add custom colors if needed
    - Pin to tailwindcss@^3.4 (NOT v4 per research)

    **PostCSS config (`postcss.config.js`):**
    - tailwindcss and autoprefixer plugins

    **Global styles (`src/styles/globals.css`):**
    Per the CONTEXT.md locked decisions and research Pattern 5:
    - @tailwind base/components/utilities directives
    - Custom CSS variables in @layer base for both :root (light) and .dark (dark) themes
    - Purple dominant: `--primary: 263 84% 55%` (maps to #7C3AED)
    - Blue secondary: `--secondary: 217 91% 60%`
    - Dark theme: deep backgrounds (`--background: 240 10% 3.9%`), collector's vault feel
    - Light theme: clean whites with purple accents
    - All standard shadcn/ui variables (card, popover, muted, accent, destructive, border, input, ring)
    - Custom gradient utility classes for purple-to-blue gradients on buttons/headers per CONTEXT.md

    **Font:** Choose a geometric/technical font fitting the dark immersive vibe (Claude's discretion per CONTEXT.md). Options: Inter (clean, geometric), Space Grotesk (technical), or JetBrains Mono for accent. Load via next/font/google for optimization.

    **next-intl setup:**
    1. `src/i18n/routing.ts`: `defineRouting({ locales: ['pt-BR'], defaultLocale: 'pt-BR' })`
    2. `src/i18n/request.ts`: `getRequestConfig` that loads messages for the requested locale
    3. `src/middleware.ts`: `createMiddleware(routing)` with matcher config excluding api, _next, static
    4. `src/messages/pt-BR.json`: Initial PT-BR messages for:
       - common: { appName, loading, error, success, save, cancel, back, search, noResults }
       - nav: { home, collection, marketplace, deals, settings, admin, login, signup, logout }
       - auth: { login, signup, email, password, name, forgotPassword, resetPassword, acceptTerms, ... }
       - theme: { dark, light, toggle }
       - errors: { required, invalidEmail, passwordTooShort, ... }
    5. Update `next.config.ts` with next-intl plugin: `createNextIntlPlugin()(config)` plus standalone output and outputFileTracingRoot

    **Root layout (`src/app/layout.tsx`):**
    - `<html lang="pt-BR" suppressHydrationWarning>` (suppressHydrationWarning for next-themes)
    - Load font via next/font/google
    - Import globals.css

    **Locale layout (`src/app/[locale]/layout.tsx`):**
    - Wrap with `NextIntlClientProvider` providing messages
    - Wrap with `ThemeProvider` from next-themes: attribute="class", defaultTheme="dark", enableSystem=false
    - Wrap with Toaster (sonner) for toast notifications
    - This layout is the shell all pages render inside

    **not-found.tsx:** Simple 404 page with PT-BR text
  </action>
  <verify>
    - `pnpm --filter web dev` starts without errors
    - Visit http://localhost:3000 — page loads with dark theme by default
    - Page shows PT-BR text (from messages/pt-BR.json)
    - Browser DevTools shows no hydration mismatch warnings
    - `components.json` exists (shadcn/ui initialized)
    - shadcn/ui Button component renders with purple accent
  </verify>
  <done>
    Next.js is configured with next-intl (PT-BR), next-themes (dark default), Tailwind CSS 3.4 with purple accent, and shadcn/ui components. The locale layout wraps all pages with theme and i18n providers.
  </done>
</task>

</tasks>

<verification>
- Dark mode is the default on first visit (no flash of light mode)
- All text is in PT-BR
- shadcn/ui components render with purple accent color
- `components.json` exists with correct aliases
- `pnpm --filter web build` produces standalone output
</verification>

<success_criteria>
The Next.js frontend is configured with next-intl (PT-BR), next-themes (dark mode default with purple accent), Tailwind CSS 3.4, and shadcn/ui component library. All essential configuration files are in place and the app renders with the correct theme and language.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-06-SUMMARY.md`
</output>
