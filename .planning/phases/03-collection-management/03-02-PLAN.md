---
phase: 03-collection-management
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/web/src/lib/api/collection.ts
  - apps/web/src/components/features/collection/series-progress-card.tsx
  - apps/web/src/app/[locale]/(collector)/collection/series-progress/page.tsx
  - apps/web/src/components/features/collection/plan-limit-banner.tsx
  - apps/web/src/app/[locale]/(collector)/collection/page.tsx
  - apps/web/src/app/[locale]/(collector)/collection/[id]/page.tsx
  - apps/web/src/components/features/catalog/catalog-card.tsx
  - apps/web/src/messages/pt-BR.json
autonomous: true
requirements:
  - COLL-05
  - COLL-06
  - COLL-07
  - SERI-05
  - SERI-06
  - SERI-07

must_haves:
  truths:
    - "When a user hits their collection limit, they see a clear message with their current count, limit, plan type, and a suggestion to upgrade"
    - "The series progress page shows a list of missing editions for each series with links to the catalog"
    - "Clicking a missing edition link takes the user to the catalog search filtered to find that entry"
    - "The catalog card 'Add to Collection' button adds the item with defaults and shows a toast"
    - "User can view and upload photos on the collection item detail page"
    - "User can see the plan limit banner when approaching or reaching the limit on the collection page"
  artifacts:
    - path: "apps/web/src/components/features/collection/plan-limit-banner.tsx"
      provides: "Upgrade CTA banner when limit is near or reached"
    - path: "apps/web/src/lib/api/collection.ts"
      provides: "API functions for missing editions, photo upload, photo delete"
    - path: "apps/web/src/components/features/collection/series-progress-card.tsx"
      provides: "Expandable missing editions panel per series"
    - path: "apps/web/src/components/features/catalog/catalog-card.tsx"
      provides: "Working Add to Collection button"
  key_links:
    - from: "series-progress-card.tsx"
      to: "/api/v1/collection/missing-editions/:seriesId"
      via: "getMissingEditions API call"
      pattern: "getMissingEditions"
    - from: "plan-limit-banner.tsx"
      to: "collection page"
      via: "rendered when stats.totalItems >= limit threshold"
      pattern: "COLLECTION_LIMITS"
    - from: "catalog-card.tsx"
      to: "/api/v1/collection"
      via: "addCollectionItem API call on button click"
      pattern: "addCollectionItem"
---

<objective>
Close all frontend gaps for Phase 3 Collection Management: wire the catalog card "Add to Collection" button (quick-add), add plan limit upgrade CTA banner, show missing editions on the series progress page with links to catalog search, and add photo upload/display on collection item detail page.

Purpose: The API endpoints from Plan 01 need frontend consumers. The existing UI is solid but missing: upgrade CTAs for plan limits, missing editions visibility, photo management, and the catalog quick-add integration.

Output: Updated collection UI with plan limit awareness, missing editions panels, photo upload on item detail, and a working catalog quick-add button.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collection-management/03-RESEARCH.md
@.planning/phases/03-collection-management/03-01-SUMMARY.md

# Key existing files to understand before editing:
@apps/web/src/lib/api/collection.ts
@apps/web/src/app/[locale]/(collector)/collection/page.tsx
@apps/web/src/app/[locale]/(collector)/collection/[id]/page.tsx
@apps/web/src/app/[locale]/(collector)/collection/series-progress/page.tsx
@apps/web/src/components/features/collection/series-progress-card.tsx
@apps/web/src/components/features/catalog/catalog-card.tsx
@apps/web/src/messages/pt-BR.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API client functions, plan limit banner, and photo management UI</name>
  <files>
    apps/web/src/lib/api/collection.ts
    apps/web/src/components/features/collection/plan-limit-banner.tsx
    apps/web/src/app/[locale]/(collector)/collection/page.tsx
    apps/web/src/app/[locale]/(collector)/collection/[id]/page.tsx
    apps/web/src/messages/pt-BR.json
  </files>
  <action>
1. **API client** (`apps/web/src/lib/api/collection.ts`):
   - Add `MissingEdition` interface: `{ id: string; title: string; editionNumber: number | null; volumeNumber: number | null; coverImageUrl: string | null; slug: string }`.
   - Add `getMissingEditions(seriesId: string): Promise<MissingEdition[]>` -- calls `GET /collection/missing-editions/${seriesId}`, returns `response.data.data`.
   - Add `uploadCollectionPhoto(itemId: string, file: File): Promise<CollectionItem>` -- creates FormData, appends `photo` field, calls `POST /collection/${itemId}/photos` with `Content-Type: multipart/form-data`, returns `response.data.data`.
   - Add `deleteCollectionPhoto(itemId: string, photoIndex: number): Promise<CollectionItem>` -- calls `DELETE /collection/${itemId}/photos/${photoIndex}`, returns `response.data.data`.
   - Update the `CollectionItem` interface to include `photoUrls: string[] | null`.

2. **Plan limit banner** (NEW file: `apps/web/src/components/features/collection/plan-limit-banner.tsx`):
   - Create a component `PlanLimitBanner` that accepts props: `{ totalItems: number; planLimit: number; planType: string }`.
   - Import `COLLECTION_LIMITS` from `@comicstrunk/contracts` (it's already exported).
   - Display logic:
     - If `totalItems >= planLimit`: Show a warning banner: "Limite de colecao atingido (X de Y itens no plano TIPO). Faca upgrade para adicionar mais."
     - If `totalItems >= planLimit * 0.8`: Show an info banner: "Voce esta chegando perto do limite (X de Y itens)."
     - Otherwise: Don't render anything.
   - Use shadcn Alert component with `variant="destructive"` for limit reached, default variant for approaching.
   - Include a "Ver planos" button/link (link to future subscription page, for now link to `#` or render as disabled text).
   - Use `useTranslations('collection.planLimit')` for all strings.

3. **Collection page** (`apps/web/src/app/[locale]/(collector)/collection/page.tsx`):
   - Import `PlanLimitBanner` and `COLLECTION_LIMITS` from contracts.
   - After the stats section, render `<PlanLimitBanner>` when stats are loaded.
   - For the plan limit, since we don't have the user's plan type on the frontend stats response, use a simple approach: assume FREE plan limit (50) for now. The banner checks `stats.totalItems` against `COLLECTION_LIMITS.FREE`.
   - **Better approach:** Add a `planLimit` and `planType` to the stats API response. In the collection service `getStats`, also return the user's plan limit and type (same subscription lookup as `checkPlanLimit`). Update the `CollectionStats` interface in the API client to include `planLimit: number` and `planType: string`.
   - Pass the plan info to `PlanLimitBanner`.
   - Also handle the plan limit error when adding items: In the existing catch blocks where `addCollectionItem` is called (the add page, not this page), detect the structured error and show the plan limit modal/banner instead of a generic error toast. This is handled on the add page/catalog card, not this page.

4. **Collection item detail page** (`apps/web/src/app/[locale]/(collector)/collection/[id]/page.tsx`):
   - Add a "Photos" section below the existing item details.
   - Display existing photos from `item.photoUrls` as a horizontal scrollable grid of thumbnails (max 5).
   - Add an "Upload photo" button that opens a file input (accept `image/*`).
   - On file selection, call `uploadCollectionPhoto(itemId, file)`, show loading state, update the item state on success, show toast.
   - Each photo thumbnail has a delete button (X icon) that calls `deleteCollectionPhoto(itemId, index)`.
   - Use existing patterns: `toast.success`/`toast.error` from sonner, loading states with disabled buttons.

5. **Translations** (`apps/web/src/messages/pt-BR.json`):
   - Add under `collection.planLimit`:
     - `"limitReached"`: `"Limite de colecao atingido ({count} de {limit} itens no plano {plan}). Faca upgrade para adicionar mais itens."`
     - `"limitApproaching"`: `"Voce esta usando {count} de {limit} itens do seu plano {plan}."`
     - `"viewPlans"`: `"Ver planos"`
   - Add under `collection.photos`:
     - `"title"`: `"Fotos"`
     - `"upload"`: `"Enviar foto"`
     - `"uploading"`: `"Enviando..."`
     - `"uploadSuccess"`: `"Foto enviada com sucesso"`
     - `"uploadError"`: `"Erro ao enviar foto"`
     - `"deleteSuccess"`: `"Foto removida"`
     - `"deleteError"`: `"Erro ao remover foto"`
     - `"maxPhotos"`: `"Maximo de {max} fotos por item"`
     - `"noPhotos"`: `"Nenhuma foto adicionada"`
  </action>
  <verify>
    - `pnpm --filter web build` compiles without errors.
    - `pnpm --filter web lint` passes.
    - Start the web app with `pnpm --filter web dev` and verify:
      - Collection page shows the plan limit banner when item count is >= 80% of the plan limit.
      - Collection item detail page shows the photos section (empty state if no photos).
      - Photo upload button is present and functional (with Cloudinary configured).
    - Translations are complete -- no missing key warnings in console.
  </verify>
  <done>
    - API client has `getMissingEditions`, `uploadCollectionPhoto`, `deleteCollectionPhoto` functions.
    - `PlanLimitBanner` component renders conditionally based on plan usage.
    - Collection page shows plan limit awareness.
    - Collection item detail page has photo upload/display/delete section.
    - Stats API returns `planLimit` and `planType` for the current user.
    - All new strings translated to PT-BR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire missing editions panel on series progress and catalog quick-add button</name>
  <files>
    apps/web/src/components/features/collection/series-progress-card.tsx
    apps/web/src/app/[locale]/(collector)/collection/series-progress/page.tsx
    apps/web/src/components/features/catalog/catalog-card.tsx
    apps/web/src/messages/pt-BR.json
  </files>
  <action>
1. **Series progress card** (`apps/web/src/components/features/collection/series-progress-card.tsx`):
   - Add an expandable "missing editions" panel to each SeriesProgressCard.
   - Add a "Ver edicoes faltantes" button/link that toggles an expanded section.
   - When expanded, call `getMissingEditions(progress.seriesId)` from the API client (lazy load on first expand, cache result in component state).
   - Display each missing edition as a compact item: cover thumbnail (small), title, edition number, and a link to the catalog entry: `/${locale}/catalog/${edition.id}`.
   - If `progress.collected === progress.totalEditions`, don't show the "missing" button (collection is complete -- show a checkmark or "Colecao completa!" badge instead).
   - If expanded and loading, show a Skeleton placeholder.
   - If expanded and no missing editions (collection complete for this series), show "Nenhuma edicao faltante".
   - Use Collapsible from shadcn/ui (already available) or simple state toggle with conditional rendering.

2. **Series progress page** (`apps/web/src/app/[locale]/(collector)/collection/series-progress/page.tsx`):
   - No major changes needed since the card handles the missing editions expansion.
   - Optional: Add a summary line at the top: "X series, Y edicoes coletadas, Z faltantes" computed from the progressList data.

3. **Catalog card quick-add** (`apps/web/src/components/features/catalog/catalog-card.tsx`):
   - Import `addCollectionItem` from `@/lib/api/collection`.
   - Import `useAuth` from `@/components/providers/auth-provider` to check if user is logged in.
   - Import `toast` from `sonner` and `useTranslations` (already imported).
   - Import `useRouter` from `next/navigation`.
   - Wire the "Add to Collection" button (the `<Plus>` icon button):
     - If user is NOT logged in: redirect to login page (`router.push(/${locale}/login)`).
     - If user IS logged in: call `addCollectionItem({ catalogEntryId: entry.id })` with defaults (quantity: 1, condition: NEW).
     - On success: show `toast.success(t('addedToCollection'))`.
     - On error: If error message contains "already in your collection", show `toast.info(t('alreadyInCollection'))`. If error message contains "Collection limit reached", show `toast.error(t('collectionLimitReached'))`. Otherwise show generic error toast.
     - Add loading state to prevent double-clicks: use a `useState<boolean>` for `addingToCollection`, disable the button and show a spinner while loading.
   - Keep the "Favorite" button as a no-op (Phase 7 feature -- leave the existing `onClick` that just prevents default).
   - Keep the "Trade" button disabled (v2 feature).

4. **Translations** (`apps/web/src/messages/pt-BR.json`):
   - Add under `collection.seriesProgress`:
     - `"viewMissing"`: `"Ver faltantes"`
     - `"hideMissing"`: `"Ocultar faltantes"`
     - `"missingEditions"`: `"Edicoes faltantes ({count})"`
     - `"noMissing"`: `"Colecao completa! Nenhuma edicao faltante."`
     - `"complete"`: `"Completa!"`
     - `"editionNumber"`: `"Edicao {number}"`
     - `"viewInCatalog"`: `"Ver no catalogo"`
   - Add under `catalog`:
     - `"addedToCollection"`: `"Adicionado a colecao!"`
     - `"alreadyInCollection"`: `"Este item ja esta na sua colecao"`
     - `"collectionLimitReached"`: `"Limite de colecao atingido. Faca upgrade para adicionar mais."`
  </action>
  <verify>
    - `pnpm --filter web build` compiles without errors.
    - `pnpm --filter web lint` passes.
    - Start web app with `pnpm --filter web dev` and verify:
      - On the series progress page, each series card with incomplete progress shows a "Ver faltantes" button.
      - Clicking "Ver faltantes" expands to show a list of missing editions (if API is running with seed data).
      - Each missing edition links to its catalog entry page.
      - On the catalog page, hovering a catalog card shows the "Add to Collection" button. Clicking it (when logged in) triggers an API call and shows a success toast.
      - Clicking "Add to Collection" when not logged in redirects to login.
      - Clicking "Add to Collection" for an item already in collection shows an info toast.
    - `pnpm type-check` passes across the monorepo.
  </verify>
  <done>
    - Series progress cards have expandable missing editions panels with lazy-loaded data.
    - Missing editions link to their catalog entry pages.
    - Complete series show a "Complete" badge instead of a "missing" button.
    - Catalog card "Add to Collection" button works: adds item with defaults, shows appropriate toasts, handles errors.
    - Unauthenticated users are redirected to login on quick-add attempt.
    - All new strings translated to PT-BR.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` (full monorepo) succeeds without errors.
2. `pnpm type-check` passes for all packages.
3. Collection page shows plan limit banner when approaching or exceeding limit.
4. Series progress page shows missing editions when expanded, with links to catalog.
5. Catalog card quick-add works end-to-end (login check, API call, toast feedback).
6. Collection item detail page shows photo section with upload/delete functionality.
7. All existing collection UI functionality (list, filters, CSV, stats) continues to work.
8. All new UI strings are in PT-BR translations with no missing key warnings.
</verification>

<success_criteria>
- Plan limit awareness is visible to users on the collection page.
- Missing editions are discoverable from the series progress page.
- Missing edition links go to catalog entries.
- Catalog browsing has a one-click "add to collection" path.
- Photos can be uploaded and displayed on collection items.
- Full monorepo builds and type-checks successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/03-collection-management/03-02-SUMMARY.md`
</output>
